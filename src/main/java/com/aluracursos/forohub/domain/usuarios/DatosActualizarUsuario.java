package com.aluracursos.forohub.domain.usuarios;

public record DatosActualizarUsuario(Long id,
                                     String nombre,
                                     String clave) {
}